name: main

on:
  workflow_dispatch:

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: Publish test coverage report
        uses: aurelien-baudet/workflow-dispatch@v2
        with:
          repo: BillKalaitzis/repo2
          ref: refs/heads/main
          workflow: main.yml
          token: ${{ secrets.TOKEN }}
          inputs: '{}'
